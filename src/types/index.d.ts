declare module 'portuguese-utils' {
    function isValidPostalCode(postalCode: string): boolean
    function isValidNIF(nif: string): boolean
    function isValidMobileNumber(mobileNumber: string): boolean
}